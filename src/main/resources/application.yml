spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/plazoleta
    username: postgres
    password: postgrespwd123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db.migration




appDescription: "Pragma Powerup"
appVersion: "1.0.0"
